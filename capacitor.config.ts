import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mbti.avatar.app',
  appName: 'MBTI Avatar Generator',
  webDir: 'dist'
};

export default config;
